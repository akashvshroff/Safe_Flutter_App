import 'package:flutter/material.dart';
import '../models/detail_model.dart';

class DetailFocus extends StatelessWidget {
  final DetailModel detail;

  DetailFocus({this.detail});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
